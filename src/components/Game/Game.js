import React from 'react';
import styles from './Game.module.scss'
import PropTypes from 'prop-types';

const Game = (props) => {
    return (
        <div>

        </div>
    );
};

Game.propTypes = {

};

export default Game;
